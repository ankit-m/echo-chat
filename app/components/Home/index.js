import React from 'react';

const Home = () => (
  <div className="text-center text-muted" style={{ marginTop: '120px' }}>
    <span className="glyphicon glyphicon-hand-left" />
    &nbsp;Add/select a friend to chat
  </div>
);

export default Home;
